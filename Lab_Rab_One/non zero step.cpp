#include "Include.h"

bool findNonZeroStep(const int& quantity)
{
	if (quantity <= 0)
	{
		cout << "The task cannot be completed if n is less than 0 or equal to 0!\n";
		cout << "Enter again:\n";
		return true;
	}
	else
	{
		return false;
	}
}